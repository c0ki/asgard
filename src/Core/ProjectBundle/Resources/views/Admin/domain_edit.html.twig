{% extends 'CoreProjectBundle:Admin:list.html.twig' %}

{% block projects %}{% endblock %}

{% block domains %}
    {{ parent() }}

    <section id="domain-edit">
        <h2>Domain {% if edit_mode %}edition{% else %}creation{% endif %}</h2>
        {{ form(form) }}
    </section>
{% endblock %}