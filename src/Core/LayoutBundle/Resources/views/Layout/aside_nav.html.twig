<nav id="tools">
    <ul>
        {% for tool in tools %}
            <li><a href="{{ path(tool.route_name) }}" class="{{ tool.class|default('icon-no') }}">{{ tool.label }}</a></li>
        {% endfor %}
    </ul>
</nav>